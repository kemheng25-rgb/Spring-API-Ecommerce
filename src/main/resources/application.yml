spring:
  application:
    name: demo-api

  # ---- Profiles: dev / prod ----
  # Default is now prod (Postgres) - see java-api/CLAUDE.md, this replaces the previous
  # "H2 in dev" convention. Pass -Dspring-boot.run.profiles=dev to get H2 back for a quick,
  # throwaway run with no local Postgres needed.
  profiles:
    active: prod

  # ---- JPA / Hibernate ----
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  # ---- Validation ----
  jackson:
    serialization:
      write-dates-as-timestamps: false
    default-property-inclusion: non_null

  # ---- Kafka: order.events, published by the outbox poller, consumed per-service by group-id ----
  kafka:
    # 9094 is Kafka's host-facing listener in docker-compose (PLAINTEXT_HOST); containers on the
    # compose network use "kafka:9092" instead, set via KAFKA_BOOTSTRAP_SERVERS in the api service.
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9094}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
    consumer:
      group-id: demo-api
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer

  # ---- RabbitMQ: notification.queue, published by NotificationPublisher after payment commits ----
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:guest}
    password: ${RABBITMQ_PASSWORD:guest}

# ---- CORS: browser-based clients (e.g. the Nuxt frontend) call this API directly from JS ----
app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3002,http://127.0.0.1:3002,http://172.21.110.15:3002}

# ---- Swagger / OpenAPI ----
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method

# ---- Logging ----
logging:
  level:
    com.example.ecommerce: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: WARN

---
# ---- DEV Profile ----
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    # DB_CLOSE_DELAY=-1 keeps this in-memory DB alive for the JVM's lifetime - without it,
    # H2 drops the entire database (including the schema) the moment the connection pool's
    # last connection closes, which silently wipes all data with no error until the next query.
    url: jdbc:h2:mem:demodb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

---
# ---- PROD Profile ----
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    # Host port is 5434, not Postgres's usual 5432 - see docker-compose.yml's db service comment
    # (a native Windows Postgres install already owns 5432 on this machine).
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5434/demodb}
    driver-class-name: org.postgresql.Driver
    username: ${DATABASE_USER:postgres}
    password: ${DATABASE_PASSWORD:postgres}
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    hibernate:
      ddl-auto: validate
